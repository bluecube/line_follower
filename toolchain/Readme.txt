This directory contains configuration for crostool-ng and as a target directory for
the generated toolchain.

Run `ct-ng build` here.

Crosstool version needs to be > 1.23.0, as of 2019-01-06 this means git version.
